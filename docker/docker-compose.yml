version: '3'
services:
  db:
    build:
      context: ../.
      dockerfile: docker/db/Dockerfile
    ports:
      - "10301:3306"
  app:
    build:
      context: ../.
      dockerfile: docker/app/Dockerfile
    depends_on:
      - db
